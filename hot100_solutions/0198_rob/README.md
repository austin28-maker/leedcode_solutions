## 解题思路

- 动态规划
    - 动态规划的的四个解题步骤是：
        - 定义子问题
            - 偷 [0..k) 房间中的最大金额
        - 写出子问题的递推关系
            - f(k) = max{ rob(k-1), nums[k-1] + rob(k-2) }
        - 确定 DP 数组的计算顺序
            - 从 f(0) 开始计算，到 f(N) 结束
        - 空间优化（可选）
            - 只需要前两个状态，所以可以使用滚动数组优化空间复杂度