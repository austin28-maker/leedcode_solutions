## 思路
- 利用递归的方法解决改题目
- 递归的出口条件是root为空，返回0
- 递归的递进条件是返回左子树的最大深度和右子树的最大深度的较大值，再加1
- 最后，返回左子树的最大深度和右子树的最大深度的较大值，再加1，就是当前节点的最大深度

## 注意事项
- 注意递归的出口条件，因为如果root为空，就直接返回0
- 注意递归的递进条件，因为如果root不为空，就递归调用左子树和右子树的最大深度，再取较大值，再加1
- 最后，返回左子树的最大深度和右子树的最大深度的较大值，再加1，就是当前节点的最大深度