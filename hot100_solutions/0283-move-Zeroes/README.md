## 获取长度

| 类型 | 获取长度方式 |
| --- | --- |
| 数组（Array） | nums.length |
| 字符串（String） | str.length() |
| 集合（如 ArrayList） | list.size() |

## 第一版思路

- 双指针：一个指针遍历数组，另一个指针记录非零元素的索引。
- 遍历数组时，遇到非零元素，就将该元素移动到记录的索引位置，同时将记录的索引加一。
- 最后，将记录的索引位置后的所有元素都设为零。