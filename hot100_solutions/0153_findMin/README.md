## 思路

- 二分查找
- 旋转排序数组：利用中间元素与最后一个元素的关系，判断旋转点在左侧还是右侧。

## 注意事项

- 开区间 (-1, n-1)
- 闭区间 [0, n-1]
    - 闭区间的写法中，二分查找的循环体内必须 nums[mid] <= nums[n - 1] ，否则会越界
    - 任何最小值恰好是最后一个元素的输入（例如单元素数组）都会导致 left 指向最后一个元素，从而判断越界
- 开闭区间的理解