## 解题思路

- 交换数组元素的位置，对比索引和元素的值是否一致
- 如果不一致，就交换到正确的索引位置
- 由于返回的是缺失的正整数，所以只有两种情况：
  - 1. 返回数组长度加1
  - 2. 返回真正缺失的正整数

## 思考

- 逐渐理解了while循环的撰写时机了，可以先把理想下的情况写出来，再考虑其余复杂情况
- 其余的复杂情况可以while循环、for循环等语句处理
- 例如，数组中缺失的正整数在数组中，返回缺失的正整数