## 解题思路

- 方法一：滑动窗口
    - right 不断向右扩张吃字符，当窗口已涵盖 t 时，while 循环让 left 不断右移尝试缩短窗口，同时记录最短答案
    - 利用数组或哈希表记录窗口内字符的出现次数，注意使用数组和使用哈希表的区别，哪个更合适

## 注意事项

- 本体采用了利用数组记录窗口内字符的出现次数，字符作为数组的索引，字符出现的次数作为数组的值
- 数组的索引范围是[0, 127]，因为ASCII码表中只有128个字符