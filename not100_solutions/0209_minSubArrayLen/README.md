## 思路

- 方法一：滑动窗口
    - 两个指针 left 和 right 表示滑动窗口的左右端点
    - sum 表示滑动窗口内的元素和
    - 遍历数组，right 指针枚举滑动窗口的右端点
    - 每次枚举 right 指针，更新 sum
    - 当 sum 大于等于 target 时，更新 ans
    - 最后返回 ans
    - 时间复杂度：O(n)
    - 空间复杂度：O(1)