## 解题思路

- 暴力枚举
    - 枚举所有可能的偏移量，统计每个偏移量下的重叠数量
    - 时间复杂度：O(n^4)

- 卷积操作
    - 将 img1 转换为 32 位整型数组 s，每个元素表示 img1 的某一行
    - 将 img2 转换为 32 位整型数组 t，每个元素表示 img2 的某一行
    - 对每个可能的偏移量 dx 和 dy，统计 s[i + dx] 左移或者右移 dy 位后 按位与 t[i] 中1的数量
    - 时间复杂度：O(n^2)